"""atlas-evals: a Python harness for evaluating atlas-core's retrieval and generation quality.

It is an external HTTP client of ``atlas-core`` — no in-process Java dependency (see ADR 0007).
"""

__version__ = "0.1.0"
